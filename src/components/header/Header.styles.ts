import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  headerContainer: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    padding: 22,
    height: 90,
    gap: 5,
  },
  headerText: {
    fontSize: 20,
    flex: 1,
    textAlign: 'center',
  },
});
